#!/usr/bin/env perl
use warnings;
use strict;
use open qw(:locale);

use YAML::XS;

local $/ = undef;               # slurp entire files
while (<>) {
    my @documents = YAML::Load($_);
    print(Dump(@documents));
}
